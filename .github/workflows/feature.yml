name: "Feature"
on:
  push:
    branches:
      - feature/**

permissions:
  id-token: write
  contents: read

jobs:
  Feature:
    uses: ./.github/workflows/terraform_init_plan.yml
    with:
      aws-assume-role-arn: "arn:aws:iam::788796860262:role/github-actions-dheimy00-pipeline"
      aws-region: "us-east-2"
